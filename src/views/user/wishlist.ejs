<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Wishlist | Beauty Essentials</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <style>
     :root {
    --primary-color: #f28bb0;
    --secondary-color: #ffedf5;
    --accent-color: #d6518c;
    --text-color: #4f3851;
    --light-gray: #fbf7fa;
    --medium-gray: #f5edf6;
    --dark-gray: #9a8ba0;
    --white: #ffffff;
    --danger: #ff5981;
    --success: #6cd9b3;
    --gold: #f9d4b4;
    --shadow: 0 10px 30px rgba(242, 139, 176, 0.15);
    --transition: all 0.4s cubic-bezier(0.25, 1, 0.5, 1);
}

body {
    font-family: 'Montserrat', sans-serif;
    background-color: var(--light-gray);
    background-image: 
        radial-gradient(circle, #ffd6e6 2px, transparent 2px),
        radial-gradient(circle, #ffd6e6 2px, transparent 2px);
    background-size: 40px 40px;
    background-position: 0 0, 20px 20px;
    margin: 0;
    padding: 0;
    color: var(--text-color);
}

.breadcrumb-wrap {
    background-color: var(--white);
    padding: 20px 0;
    box-shadow: 0 4px 15px rgba(242, 139, 176, 0.1);
    position: relative;
    overflow: hidden;
}

.breadcrumb-wrap::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(to right, var(--primary-color), var(--accent-color), var(--gold));
}

.breadcrumb {
    display: flex;
    align-items: center;
}

.breadcrumb a {
    color: var(--text-color);
    text-decoration: none;
    font-weight: 500;
    transition: var(--transition);
    position: relative;
    letter-spacing: 0.5px;
    font-size: 0.95rem;
}

.breadcrumb a:not(:first-child)::before {
    transition: var(--transition);
}

.breadcrumb a:hover {
    color: var(--accent-color);
}

.breadcrumb span {
    margin: 0 15px;
    color: var(--dark-gray);
    font-size: 12px;
}

.page-title {
    text-align: center;
    margin: 50px 0 40px;
    color: var(--accent-color);
    font-weight: 700;
    letter-spacing: 1px;
    position: relative;
    font-family: 'Playfair Display', serif;
    font-size: 2.5rem;
    text-transform: none;
}

.page-title span {
    position: relative;
    display: inline-block;
    padding: 0 20px;
}

.page-title span::before,
.page-title span::after {
    content: '♥';
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    color: var(--primary-color);
    font-size: 0.8em;
}

.page-title span::before {
    left: -15px;
}

.page-title span::after {
    right: -15px;
}

.page-title::after {
    content: '';
    position: absolute;
    bottom: -15px;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 3px;
    background: linear-gradient(to right, var(--gold), var(--primary-color), var(--accent-color), var(--primary-color), var(--gold));
    border-radius: 10px;
}

.wishlist-container {
    background-color: var(--white);
    border-radius: 30px;
    box-shadow: var(--shadow);
    padding: 40px;
    margin-bottom: 60px;
    overflow: hidden;
    position: relative;
    border: 1px solid rgba(242, 139, 176, 0.1);
}

.wishlist-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(to right, var(--gold), var(--primary-color), var(--accent-color));
}

.table {
    margin-top: 20px;
    background-color: var(--white);
    border-radius: 15px;
    overflow: hidden;
    border-collapse: separate;
    border-spacing: 0;
    width: 100%;
}

.table th {
    background-color: var(--secondary-color);
    color: var(--accent-color);
    padding: 22px 18px;
    text-align: center;
    font-weight: 600;
    border: none;
    letter-spacing: 1px;
    font-size: 1rem;
    position: relative;
    text-transform: uppercase;
    font-family: 'Montserrat', sans-serif;
}

.table th:after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 3px;
    background: linear-gradient(to right, var(--primary-color), var(--accent-color));
    border-radius: 10px;
}

.table td {
    padding: 25px 18px;
    vertical-align: middle;
    border-top: 1px solid rgba(214, 81, 140, 0.08);
    text-align: center;
}

.product-thumbnail {
    width: 130px;
}

.product-thumbnail img {
    width: 100px;
    height: 100px;
    object-fit: cover;
    border-radius: 20px;
    transition: var(--transition);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
    padding: 4px;
    background: linear-gradient(135deg, #fff, #ffeef6);
    border: 1px solid #ffe0ed;
    transform: rotate(-2deg);
}

.product-thumbnail img:hover {
    transform: translateY(-5px) rotate(0deg);
    box-shadow: 0 12px 25px rgba(242, 139, 176, 0.25);
}

.product-name a {
    color: var(--text-color);
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
    position: relative;
    display: inline-block;
    font-size: 1.1rem;
    letter-spacing: 0.3px;
}

.product-name a:after {
    content: '';
    position: absolute;
    bottom: -3px;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(to right, var(--primary-color), var(--accent-color));
    transition: var(--transition);
}

.product-name a:hover {
    color: var(--accent-color);
}

.product-name a:hover:after {
    width: 100%;
}

.product-category {
    font-size: 0.85rem;
    color: var(--dark-gray);
    margin-top: 8px;
    font-style: italic;
    letter-spacing: 0.5px;
}

.product-price {
    font-weight: 700;
    color: var(--accent-color);
    font-size: 1.3rem;
    position: relative;
    display: inline-block;
}

.product-price span {
    position: relative;
}

.product-price span:after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 100%;
    height: 2px;
    border-radius: 5px;
    background: linear-gradient(to right, var(--gold), var(--primary-color), var(--accent-color), var(--primary-color), var(--gold));
    background-size: 200% 100%;
    animation: shimmer 3s infinite;
}

@keyframes shimmer {
    0% {
        background-position: 100% 0;
    }
    100% {
        background-position: 0 0;
    }
}

.shade-selection {
    max-width: 320px;
    margin: 0 auto;
    padding: 20px;
    background-color: #fef6fa;
    border-radius: 20px;
    box-shadow: 0 5px 15px rgba(242, 139, 176, 0.15);
    border: 1px solid #ffe0ed;
    transition: all 0.3s ease;
}

.section-title {
    text-align: center;
    color: var(--accent-color);
    margin-bottom: 18px;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 1px;
    font-family: 'Montserrat', sans-serif;
    text-transform: uppercase;
    position: relative;
}

.section-title::after {
    content: '';
    position: absolute;
    bottom: -8px;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 2px;
    background: linear-gradient(to right, var(--primary-color), var(--accent-color));
    border-radius: 10px;
}

.shade-options {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    justify-content: center;
    margin-top: 15px;
}

.shade-btn {
    padding: 9px 14px;
    background-color: white;
    border: 2px solid var(--primary-color);
    border-radius: 20px;
    color: var(--text-color);
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 0.85rem;
    font-weight: 500;
    text-transform: capitalize;
    box-shadow: 0 3px 8px rgba(0, 0, 0, 0.05);
}

.shade-btn:hover {
    background-color: var(--primary-color);
    color: white;
    transform: translateY(-3px);
    box-shadow: 0 5px 12px rgba(242, 139, 176, 0.3);
}

.shade-btn.selected {
    background: linear-gradient(to right, var(--primary-color), var(--accent-color));
    color: white;
    border-color: transparent;
    box-shadow: 0 5px 15px rgba(214, 81, 140, 0.3);
    transform: translateY(-3px);
}

.shade-btn.disabled {
    background-color: #f7f7f7;
    color: #aaa;
    cursor: not-allowed;
    border-color: #e5e5e5;
    text-decoration: line-through;
    opacity: 0.7;
    box-shadow: none;
}

.selected-shade-stock {
    text-align: center;
    margin-top: 15px;
    font-size: 0.85rem;
    color: var(--dark-gray);
    font-style: italic;
}

.btn-action {
    padding: 12px 22px;
    border: none;
    cursor: pointer;
    border-radius: 30px;
    font-size: 0.95rem;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: var(--transition);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
    letter-spacing: 0.5px;
    text-transform: uppercase;
}

.btn-cart {
    background: linear-gradient(45deg, #65d0be, #6cd9b3, #65d0be);
    background-size: 200% 100%;
    color: var(--white);
    position: relative;
    z-index: 1;
    animation: subtle-shift 3s infinite alternate;
}

@keyframes subtle-shift {
    0% {
        background-position: 0% 50%;
    }
    100% {
        background-position: 100% 50%;
    }
}

.btn-cart:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, #6cd9b3, #65d0be, #6cd9b3);
    background-size: 200% 100%;
    border-radius: 30px;
    opacity: 0;
    z-index: -1;
    transition: var(--transition);
}

.btn-cart:hover:before {
    opacity: 1;
}

.btn-cart:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 20px rgba(108, 217, 179, 0.3);
}

.btn-remove {
    background: linear-gradient(45deg, #ff5981, #ff82a9, #ff5981);
    background-size: 200% 100%;
    color: var(--white);
    position: relative;
    z-index: 1;
}

.btn-remove:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, #ff82a9, #ff5981, #ff82a9);
    background-size: 200% 100%;
    border-radius: 30px;
    opacity: 0;
    z-index: -1;
    transition: var(--transition);
}

.btn-remove:hover:before {
    opacity: 1;
}

.btn-remove:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 20px rgba(255, 89, 129, 0.3);
}

.btn-action i {
    margin-right: 10px;
    font-size: 1em;
}

.empty-wishlist {
    text-align: center;
    padding: 70px 30px;
    background: linear-gradient(135deg, #ffedf5, #fff6fe);
    border-radius: 20px;
    box-shadow: inset 0 2px 10px rgba(242, 139, 176, 0.1);
}

.empty-wishlist i {
    font-size: 6rem;
    color: var(--primary-color);
    display: block;
    margin-bottom: 30px;
    opacity: 0.8;
    animation: floating 3s ease-in-out infinite;
}

@keyframes floating {
    0% {
        transform: translateY(0) rotate(-5deg);
    }
    50% {
        transform: translateY(-15px) rotate(5deg);
    }
    100% {
        transform: translateY(0) rotate(-5deg);
    }
}

.empty-wishlist p {
    font-size: 1.4rem;
    color: var(--text-color);
    margin-bottom: 30px;
    font-weight: 500;
    font-family: 'Playfair Display', serif;
}

.btn-shop-now {
    display: inline-block;
    background: linear-gradient(45deg, var(--accent-color), var(--primary-color));
    color: var(--white);
    padding: 14px 35px;
    border-radius: 30px;
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
    position: relative;
    z-index: 1;
    box-shadow: 0 8px 20px rgba(214, 81, 140, 0.3);
    letter-spacing: 1px;
    text-transform: uppercase;
    font-size: 0.95rem;
}

.btn-shop-now:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, var(--primary-color), var(--accent-color));
    border-radius: 30px;
    opacity: 0;
    z-index: -1;
    transition: var(--transition);
}

.btn-shop-now:hover:before {
    opacity: 1;
}

.btn-shop-now:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 25px rgba(214, 81, 140, 0.4);
    color: var(--white);
}

.notification {
    display: none;
    position: fixed;
    bottom: 30px;
    right: 30px;
    background: linear-gradient(to right, var(--success), #75e0c0);
    color: var(--white);
    padding: 20px 30px;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
    z-index: 1000;
    min-width: 340px;
    transform: translateY(20px);
    opacity: 0;
    transition: transform 0.5s, opacity 0.5s;
}

.notification.show {
    transform: translateY(0);
    opacity: 1;
}

.notification-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.notification-message {
    display: flex;
    align-items: center;
}

.notification-message i {
    margin-right: 18px;
    font-size: 1.5rem;
    color: var(--white);
}

.close-notification {
    background: none;
    border: none;
    color: var(--white);
    font-size: 24px;
    cursor: pointer;
    opacity: 0.7;
    transition: opacity 0.3s;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
}

.close-notification:hover {
    opacity: 1;
}

.error-notification {
    background: linear-gradient(to right, var(--danger), #ff82a9);
}

/* Custom Scrollbar */
::-webkit-scrollbar {
    width: 10px;
    height: 10px;
}

::-webkit-scrollbar-track {
    background-color: var(--secondary-color);
}

::-webkit-scrollbar-thumb {
    background: linear-gradient(to bottom, var(--primary-color), var(--accent-color));
    border-radius: 10px;
}

::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(to bottom, var(--accent-color), var(--primary-color));
}

/* Sweet Alert Customization */
.swal2-popup {
    border-radius: 25px !important;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1) !important;
    padding: 2.5rem !important;
    border: 1px solid #ffe0ed !important;
}

.swal2-title {
    color: var(--accent-color) !important;
    font-weight: 600 !important;
    font-family: 'Montserrat', sans-serif !important;
}

.swal2-icon {
    border-color: var(--primary-color) !important;
    color: var(--primary-color) !important;
}

.swal2-styled.swal2-confirm {
    background: linear-gradient(to right, var(--danger), #ff82a9) !important;
    border-radius: 30px !important;
    box-shadow: 0 8px 20px rgba(255, 89, 129, 0.3) !important;
    font-weight: 600 !important;
    padding: 12px 30px !important;
    transition: var(--transition) !important;
}

.swal2-styled.swal2-confirm:hover {
    transform: translateY(-3px) !important;
    box-shadow: 0 12px 25px rgba(255, 89, 129, 0.4) !important;
}

.swal2-styled.swal2-cancel {
    border-radius: 30px !important;
    font-weight: 600 !important;
    padding: 12px 30px !important;
    transition: var(--transition) !important;
}

.swal2-styled.swal2-cancel:hover {
    transform: translateY(-3px) !important;
}

/* Responsive Styles */
@media (max-width: 991px) {
    .wishlist-container {
        padding: 30px 20px;
    }

    .page-title {
        font-size: 2.2rem;
        margin: 40px 0 30px;
    }

    .table thead {
        display: none;
    }

    .table,
    .table tbody,
    .table tr,
    .table td {
        display: block;
        width: 100%;
    }

    .table tr {
        margin-bottom: 30px;
        border: 1px solid #ffe0ed;
        border-radius: 20px;
        padding: 25px 20px;
        position: relative;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
        background-color: #fff;
    }

    .table td {
        text-align: right;
        padding: 15px 0;
        border: none;
        position: relative;
        padding-left: 50%;
    }

    .table td:before {
        content: attr(data-title);
        position: absolute;
        left: 0;
        width: 45%;
        padding-right: 15px;
        font-weight: 600;
        text-align: left;
        color: var(--accent-color);
    }

    .product-thumbnail,
    .product-thumbnail img {
        width: 100%;
        height: auto;
        text-align: center;
    }

    .product-thumbnail {
        padding-left: 0 !important;
        text-align: center !important;
        margin-bottom: 20px;
    }

    .product-thumbnail:before {
        display: none;
    }

    .product-thumbnail img {
        width: 140px;
        height: 140px;
    }

    .product-name {
        text-align: center !important;
    }

    .product-name a {
        font-size: 1.2rem;
    }

    .size-select {
        margin: 0 0 0 auto;
    }

    .notification {
        min-width: calc(100% - 60px);
        right: 30px;
    }
}

@media (max-width: 576px) {
    .wishlist-container {
        padding: 25px 15px;
        border-radius: 20px;
    }

    .page-title {
        font-size: 1.8rem;
        margin: 30px 0 25px;
    }

    .table td {
        padding-left: 40%;
    }

    .table td:before {
        width: 35%;
    }

    .btn-action {
        padding: 10px 18px;
        font-size: 0.85rem;
    }

    .product-thumbnail img {
        width: 110px;
        height: 110px;
    }

    .notification {
        padding: 15px 20px;
        min-width: calc(100% - 40px);
        right: 20px;
        bottom: 20px;
    }

    .notification-message i {
        font-size: 1.2rem;
    }

    .empty-wishlist i {
        font-size: 4rem;
    }

    .empty-wishlist p {
        font-size: 1.2rem;
    }

    .btn-shop-now {
        padding: 12px 25px;
    }
}

/* Add some cosmetic-specific enhancements */
.shade-btn {
    position: relative;
    overflow: hidden;
}

.shade-btn::after {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(255,255,255,0.3) 0%, rgba(255,255,255,0) 70%);
    transform: rotate(45deg);
    opacity: 0;
    transition: opacity 0.5s;
}

.shade-btn:hover::after {
    opacity: 1;
}

.table tr {
    transition: transform 0.3s;
}

.table tr:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 30px rgba(242, 139, 176, 0.2);
}

/* Add some decorative elements */
.wishlist-container::after {
    content: "";
    position: absolute;
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(255,229,245,0.6) 0%, rgba(255,255,255,0) 70%);
    top: -50px;
    right: -50px;
    border-radius: 50%;
    z-index: -1;
}

.wishlist-container::before {
    content: "";
    position: absolute;
    width: 150px;
    height: 150px;
    background: radial-gradient(circle, rgba(255,229,245,0.6) 0%, rgba(255,255,255,0) 70%);
    bottom: -50px;
    left: -50px;
    border-radius: 50%;
    z-index: -1;
}

/* Add makeup-inspired details */
.product-price::before {
    content: "♡";
    position: absolute;
    left: -25px;
    top: 0;
    color: var(--primary-color);
    font-size: 0.9rem;
}

.product-price::after {
    content: "♡";
    position: absolute;
    right: -25px;
    top: 0;
    color: var(--primary-color);
    font-size: 0.9rem;
}
    </style>
</head>

<body>
    <%- include("../partials/user/navbar.ejs") %>
        <main class="main">
            <div class="page-header breadcrumb-wrap">
                <div class="container">
                    <div class="breadcrumb">
                        <a href="/home" rel="nofollow"><i class="fas fa-home mr-2"></i>Home</a>
                        <span></span>
                        <a href="/shop" rel="nofollow">Shop</a>
                        <span></span>
                        <a href="/wishlist" rel="nofollow">My Wishlist</a>
                    </div>
                </div>
            </div>

            <div id="notification" class="notification" role="alert">
                <div class="notification-content">
                    <div class="notification-message">
                        <i class="fas fa-check-circle"></i>
                        <span id="notification-message"></span>
                    </div>
                    <button onclick="hideNotification()" class="close-notification"
                        aria-label="Close notification">×</button>
                </div>
            </div>

            <section class="mt-50 mb-50">
                <div class="container">
                    <h2 class="page-title"><span>My Wishlist</span></h2>
                    <div class="row">
                        <div class="col-12">
                            <div class="wishlist-container">
                                <div class="table-responsive">
                                    <table class="table shopping-summery text-center clean">
                                        <% if (wishlist && wishlist.length > 0) { %>
                                            <thead>
                                                <tr class="main-heading">
                                                    <th scope="col">Image</th>
                                                    <th scope="col">Product</th>
                                                    <th scope="col">Price</th>
                                                    <th scope="col">Shade</th>
                                                    <th scope="col">Add to Cart</th>
                                                    <th scope="col">Remove</th>
                                                </tr>
                                            </thead>
                                        <% } %>
                                        <tbody>
                                            <% if (wishlist && wishlist.length > 0) { %>
                                                <% for (let item of wishlist) { %>
                                                    <% if (item.productId) { %>
                                                        <tr>
                                                            <td class="image product-thumbnail" data-title="Product">
                                                                <img src="/uploads/re-image/<%= item.productId.productImage[0] %>" />
                                                            </td>
                                                            <td class="product-des product-name" data-title="Description">
                                                                <h5 class="product-name">
                                                                    <a href="/productDetails/<%= item.productId._id %>">
                                                                        <%= item.productId.productName %>
                                                                    </a>
                                                                </h5>
                                                                <p class="product-category font-xs">
                                                                    <%= item.productId.category.name %>
                                                                </p>
                                                            </td>
                                                            <td class="price product-price" data-title="Price">₹<span>
                                                                    <%= item.productId.salePrice %>
                                                                </span></td>
                                                            <td data-title="Shade">
                                                                <div class="shade-selection">
                                                                    <h3 class="section-title">Select Shade</h3>
                                                                    <div class="shade-options">
                                                                        <% if (item.productId.shadeVariants && item.productId.shadeVariants.length > 0) { %>
                                                                            <% item.productId.shadeVariants.forEach(variant => { %>
                                                                                <button
                                                                                    class="shade-btn <%= variant.quantity === 0 ? 'disabled' : '' %>"
                                                                                    data-shade="<%= variant.shade %>"
                                                                                    data-quantity="<%= variant.quantity %>"
                                                                                    data-product-id="<%= item.productId._id %>"
                                                                                    <%= variant.quantity === 0 ? 'disabled' : '' %>>
                                                                                    <%= variant.shade %>
                                                                                </button>
                                                                            <% }); %>
                                                                        <% } else { %>
                                                                            <p>No shades available</p>
                                                                        <% } %>
                                                                    </div>
                                                                    <input type="hidden" id="selected-shade-<%= item.productId._id %>"
                                                                        name="selectedShade" value="">
                                                                    <p class="selected-shade-stock"></p>
                                                                </div>
                                                            </td>
                                                            <td class="text-center" data-title="Add to Cart">
                                                                <button onclick="addToCartFromWishlist('<%= item.productId._id %>')"
                                                                    class="btn-action btn-cart">
                                                                    <i class="fas fa-shopping-cart"></i> Add to Cart
                                                                </button>
                                                            </td>
                                                            <td class="action" data-title="Remove">
                                                                <button onclick="confirmRemove('<%= item.productId._id %>')"
                                                                    class="btn-action btn-remove">
                                                                    <i class="fas fa-trash"></i> Remove
                                                                </button>
                                                            </td>
                                                        </tr>
                                                    <% } %>
                                                <% } %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="6" class="empty-wishlist">
                                                        <i class="fas fa-heart-broken"></i>
                                                        <p>Your wishlist is empty</p>
                                                        <a href="/shop" class="btn-shop-now">Shop Now</a>
                                                    </td>
                                                </tr>
                                            <% } %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </main>

        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <script>
            document.addEventListener('DOMContentLoaded', function () {
                const shadeButtons = document.querySelectorAll('.shade-btn');

                shadeButtons.forEach(button => {
                    button.addEventListener('click', function () {
                        const productId = this.getAttribute('data-product-id');
                        const shade = this.getAttribute('data-shade');
                        const quantity = this.getAttribute('data-quantity');


                        document.querySelectorAll(`.shade-btn[data-product-id="${productId}"]`).forEach(btn => {
                            btn.classList.remove('selected');
                        });


                        this.classList.add('selected');


                        document.getElementById(`selected-shade-${productId}`).value = shade;

                        const stockInfo = document.querySelector(`.selected-shade-stock[data-product-id="${productId}"]`);
                        if (stockInfo) {
                            stockInfo.textContent = `Stock: ${quantity}`;
                        }
                    });
                });
            });

            function addToCartFromWishlist(productId) {
                const selectedShade = document.getElementById(`selected-shade-${productId}`).value;

                if (!selectedShade) {
                    showNotification('Please select a shade', true);
                    return;
                }

                fetch('/addToCart', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        productId: productId,
                        shade: selectedShade,
                        quantity: 1
                    })
                })
                    .then(response => response.json())
                    .then(data => {
                        if (data.status) {
                            fetch(`/removeFromWishlist?productId=${productId}`)
                                .then(() => {
                                    showNotification('Product added to cart successfully!');
                                    setTimeout(() => {
                                        window.location.reload();
                                    }, 2000);
                                });
                        } else {
                            showNotification(data.message || 'Error adding product to cart', true);
                        }
                    })
                    .catch(error => {
                        console.error('Error:', error);
                        showNotification('Error adding product to cart', true);
                    });
            }

            function confirmRemove(productId) {
                Swal.fire({
                    title: "Remove from Wishlist?",
                    text: "This item will be removed from your wishlist",
                    icon: "question",
                    showCancelButton: true,
                    confirmButtonColor: "#fd3758",
                    cancelButtonColor: "#6c757d",
                    confirmButtonText: "Yes, remove it",
                    cancelButtonText: "Keep it",
                    background: "#ffffff",
                    borderRadius: "15px",
                    customClass: {
                        confirmButton: "btn btn-danger px-4 py-2",
                        cancelButton: "btn btn-secondary px-4 py-2"
                    }
                }).then((result) => {
                    if (result.isConfirmed) {
                        window.location.href = `/removeFromWishlist?productId=${productId}`;
                    }
                });
            }

            function showNotification(message, isError = false) {
                const notification = document.getElementById('notification');
                const messageElement = document.getElementById('notification-message');

                if (window.notificationTimeout) {
                    clearTimeout(window.notificationTimeout);
                }

                messageElement.textContent = message;

                if (isError) {
                    notification.classList.add('error-notification');
                    notification.querySelector('.notification-message i').className = 'fas fa-exclamation-circle';
                } else {
                    notification.classList.remove('error-notification');
                    notification.querySelector('.notification-message i').className = 'fas fa-check-circle';
                }

                notification.style.display = 'block';
                setTimeout(() => {
                    notification.classList.add('show');
                }, 10);

                window.notificationTimeout = setTimeout(() => {
                    hideNotification();
                }, 5000);
            }

            function hideNotification() {
                const notification = document.getElementById('notification');
                notification.classList.remove('show');
                setTimeout(() => {
                    notification.style.display = 'none';
                }, 300);
            }
        </script>
        <%- include("../partials/user/footer.ejs") %>
</body>

</html>